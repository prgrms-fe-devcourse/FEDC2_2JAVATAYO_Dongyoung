import styled from "@emotion/styled";

export const Wrapper = styled.span`
  position: absolute;
  bottom: 0;
  right: -3px;
`;

export const DropBox = styled.div`
  width: 112px;
  font-size: 12px;
  padding: 0;
`;
